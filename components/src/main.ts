// Entry point for all Svelte web components.
// Importing a custom-element Svelte component auto-registers it in the browser,
// so Hugo pages can use <hello-world> (or any other tag) without any setup code.

import "./HelloWorld.svelte";

// Add future components here:
// import './RssFeed.svelte';
