// Minimal config — primarily for editor/IDE Svelte language server support.
// The actual build config lives in vite.config.ts.
export default {
  compilerOptions: {
    customElement: true,
  },
};
